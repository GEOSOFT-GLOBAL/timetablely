import * as React from "react";

interface DatabaseManagerProps {
  propName: type;
}

const DatabaseManager: React.FC<DatabaseManagerProps> = ({ propName }) => {
  return (
    <div>
      <div>hi</div>
    </div>
  );
};

export default DatabaseManager;
