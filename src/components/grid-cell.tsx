import * as React from "react";

interface GridCellProps {
  propName: type;
}

const GridCell: React.FC<GridCellProps> = ({ propName }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default GridCell;
